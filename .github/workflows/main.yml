on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - name: CPU Model
        run: cat /proc/cpuinfo  | grep 'name'| uniq
      - name: CPU Cores
        run: cat /proc/cpuinfo  | grep process| wc -l
      - name: Disk Space
        run: df -h
      - name: RAM MB
        run: free --mega
      - name: Download ZIP file
        run: curl -L https://github.com/CarlaUnreal/UnrealEngine/archive/refs/heads/carla.zip -o UnrealEngine5.zip
      - name: LS
        run: ls -la
